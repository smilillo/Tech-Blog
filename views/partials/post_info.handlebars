<div class="card my-5">
    <div class="card-content">
        <div class="media-content">
            <p class="title has-text-left"><a href="/post/{{id}}">{{title}}</a></p>
            <p class="subtitle is-6 has-text-left">{{user.username}}</p>
        </div>

        <div class="content has-text-left">
            {{post_text}}
        </div>
        <div>
            <p class="has-text-left"><a href="post/{{id}}">{{comments.length}}</a></p>
        </div>

    </div>
</div>